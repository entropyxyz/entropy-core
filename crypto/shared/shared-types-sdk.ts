/*
 Generated by typeshare 1.6.0
*/

/**
 * X25519 public key used by the client in non-interactive ECDH to authenticate/encrypt
 * interactions with the threshold server (eg distributing threshold shares).
 */
export type X25519PublicKey = [number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number, number];

/** body of a signature generation request by the user to the entropy network */
export interface SigRequest {
	/** sig_hash. this is a hash of the message to be signed */
	sig_hash: number[];
}

export interface ValidatorInfo {
	x25519_public_key: X25519PublicKey;
	ip_address: number[];
	tss_account: number[];
}

/** The message sent from pallets::propagation::post() to the signing-client. */
export interface Message {
	sig_request: SigRequest;
	account: number[];
	validators_info: ValidatorInfo[];
}

